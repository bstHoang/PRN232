@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model List<Authen_Demo.Models.User>
@{
}
<h2>Welcome @ViewBag.Account!</h2>
<form asp-action="Grade" asp-controller="StudentHome" method="get">
    <button type="submit">Check my Grade</button>
</form>
<h3>Student list</h3>
<table border="1">
    <thead>
        <tr>
            <th>ID</th>
            <th>Account Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.UserId</td>
                <td>@user.Account</td>
            </tr>
        }
    </tbody>
</table>
<form asp-action="Logout" asp-controller="Home" method="post">
    <button type="submit">Logout</button>
</form>